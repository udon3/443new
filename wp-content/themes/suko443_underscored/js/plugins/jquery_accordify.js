/*objectives: write a simple jquery accordion plugin
	dependencies: jQuery library
	functionality:
		convert a set of headings(e.g. h3) and content containers(e.g. div) and turn it into accordion
	$(collection_set).accordify({params})
		options:
			header - element selector
			panel - element selector
			initial_state - all open, all closed, first open
			multiple_open - panels can be opened independently


*/

(function($){

	$.fn.accordify = function(){
		//


	};//end of $.fn.accordify assignment

})(jQuery);